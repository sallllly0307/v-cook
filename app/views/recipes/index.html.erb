<% @ranks.each.with_index(1) do |rank, i| %>
  <%= "第#{i}位" %>
  <img src="<%= "/user_images/#{rank.user.image_name}" %>">
  <%= link_to(rank.user.name, "/users/#{rank.user.id}") %>
  <%= link_to(rank.title, "/recipes/#{rank.id}") %>
<% end %>